# @runspace/ui is published to a local Verdaccio during development —
# see docs/PUBLISHING.md. That registry is deliberately NOT set here:
# forcing it made `npm install` in a clone fail with a connection refused,
# reported by npm as "Exit handler never called", which points nowhere near
# the cause. Set it in your own ~/.npmrc, or export npm_config_registry.
