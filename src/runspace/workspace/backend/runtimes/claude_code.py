"""Claude Code CLI runtime adapter."""

from __future__ import annotations

import asyncio
import json
import logging
import os
import re
import shutil
import sys
from collections.abc import AsyncIterator
from pathlib import Path
from typing import TYPE_CHECKING

from ..caller import caller_role
from ._failure import failure_text

if TYPE_CHECKING:  # pragma: no cover
    from ..app_registry import AgentApp, AppRegistry

log = logging.getLogger(__name__)

DEFAULT_TIMEOUT_S = float(os.environ.get("RUNSPACE_CLI_TIMEOUT", "120"))

# Per-line ceiling for the CLI's stream-json output. Generous because the line
# is an event, and an event carrying a tool result is as large as that result;
# bounded because a runaway process should not be able to exhaust memory.
STREAM_LINE_LIMIT = int(os.environ.get("RUNSPACE_CLI_LINE_LIMIT", 16 * 1024 * 1024))
CLAUDE_BIN_ENV = "CLAUDE_CODE_BIN"

# Who is asking, for `models:` selection. A host that distinguishes callers
# (an owner from a shared demo seat) sets this per request; runspace never
# learns what an account is, only that turns can be labelled.
#
#     from runspace.workspace.backend.runtimes.claude_code import caller_role
#     token = caller_role.set("demo")
#     try: ...
#     finally: caller_role.reset(token)
CLAUDE_BIN_DEFAULT = "claude"
DEFAULT_PERMISSION_MODE = "plan"


def get_or_create_agent(registry: AppRegistry, app: AgentApp):  # noqa: ARG001
    if app._agent is None:
        app._agent = {"runtime": "claude_code"}
    return app._agent


def build_gate_manager(registry: AppRegistry, app: AgentApp):  # noqa: ARG001
    return None


def _resolve_cwd(app: AgentApp) -> str:
    if app.workspace_path:
        return app.workspace_path
    if app.soul_path:
        guess = str(Path(app.soul_path).parent.parent)
        log.warning("[claude_code] app=%s has no workspace_path; falling back to %s", app.id, guess)
        return guess
    return os.getcwd()


_SHIM_MARKER = "# generated by runspace — delete this line to keep your own"


def _ensure_dispatcher(cwd: str, app: AgentApp, registry: AppRegistry | None = None) -> None:
    """Make the agent's own tools reachable from the shell.

    A CLI harness is not handed a tools array — it drives its own loop and
    reaches the outside world through Bash. An agent moved onto one from an
    in-process runtime therefore arrives with no tools at all unless something
    puts them back within reach, and the failure is quiet: the agent is told
    tools exist, cannot call any, and answers from memory instead.

    So the runtime writes the dispatcher rather than each workspace shipping
    its own copy. The interpreter is written in at generation time — it is the
    one already running this server, so it can import both the framework and
    the tools by construction, which no search of candidate paths can promise.

    A `tools` file already in the workspace is never touched, generated or not:
    a workspace that wrote its own has a reason, and clobbering it would be a
    silent downgrade. Ours is rewritten only when its content has drifted from
    what the configuration now says.
    """
    path = Path(cwd) / "tools"

    # Every tree in this workspace, not just this agent's. Agents commonly
    # share a workspace directory, and the dispatcher is one file in it — built
    # per-agent, whichever spoke last would leave the others pointing at trees
    # that are not theirs.
    #
    # Loading everything and scoping nothing is deliberate. Which agent may
    # call which tool is a gate, enforced by the per-app allowlist the harness
    # applies before the command runs. Scoping here as well would put the same
    # rule in two places and let them disagree.
    siblings = [app]
    if registry is not None:
        siblings = [
            a for a in getattr(registry, "apps", {}).values() if _resolve_cwd(a) == cwd
        ] or [app]

    dirs: list[str] = []
    for a in siblings:
        for d in [a.tools_dir, *a.shared_tools_dirs]:
            # Config paths are joined, not resolved: `tenants/x/../../agents/y`
            # works but makes the shim unreadable and two spellings of one
            # directory look like two directories.
            d = os.path.normpath(d) if d else d
            if d and d not in dirs:
                dirs.append(d)
    if not dirs:
        return

    # A directory of that name is the workspace's own layout, not a stale shim.
    if path.is_dir():
        log.warning(
            "[claude_code] app=%s cannot place a dispatcher: %s is a directory", app.id, path
        )
        return

    # Tools import the application they belong to — `from services...`,
    # `from agents...`. The server can resolve those by construction; a bare
    # subprocess cannot, and the loader reports the miss per file and carries
    # on, so the agent ends up holding some of its tools and never learns which
    # ones are gone. Hand down the path the server is actually running with.
    import_path = os.pathsep.join(p for p in sys.path if p)

    # Ambient context does not cross a process boundary. In-process a tool
    # reads `get_context("tenant_id")` from a ContextVar the server sets per
    # request; the subprocess starts with it empty, and the same tool that was
    # correct on another runtime either refuses or — worse, where it does not
    # check — answers for the wrong tenant.
    context = json.dumps({"tenant_id": getattr(registry, "tenant_id", "") or ""})

    body = (
        "#!/usr/bin/env bash\n"
        f"{_SHIM_MARKER}\n"
        "set -euo pipefail\n"
        f'export RUNSPACE_TOOLS_ROOT="{cwd}"\n'
        f'export RUNSPACE_TOOL_DIRS="{",".join(dirs)}"\n'
        f'export PYTHONPATH="{import_path}"\n'
        f"export RUNSPACE_TOOL_CONTEXT='{context}'\n"
        f'exec "{sys.executable}" -m runspace.workspace.backend.runtimes.tools_cli "$@"\n'
    )

    if path.exists():
        try:
            current = path.read_text(encoding="utf-8")
        except OSError:
            return
        if _SHIM_MARKER not in current or current == body:
            return

    try:
        # A declared workspace that does not exist yet is a directory to make,
        # not a reason to fail. Agents whose tool trees share a name — two
        # desks each with a `generate_report` — need separate workspaces to
        # keep separate dispatchers, and those directories exist only because
        # the configuration asked for them.
        path.parent.mkdir(parents=True, exist_ok=True)
        path.write_text(body, encoding="utf-8")
        path.chmod(0o755)
    except OSError as e:
        # Not fatal: the agent still runs, with the CLI's own tools only.
        log.warning("[claude_code] app=%s could not write %s: %s", app.id, path, e)


def _resolve_bin() -> str:
    return os.environ.get(CLAUDE_BIN_ENV) or shutil.which(CLAUDE_BIN_DEFAULT) or CLAUDE_BIN_DEFAULT


def _resolve_timeout(app: AgentApp) -> float:
    """Seconds this agent's turn may take: `cli_timeout_s`, else the global.

    A chat turn should give up quickly; a routine that calls four tools and
    writes a thousand words needs minutes. One global value serves neither.
    Non-positive values are ignored — nothing means "give up immediately".
    """
    raw = (app.gates_config or {}).get("cli_timeout_s")
    if raw is not None:
        try:
            seconds = float(raw)
        except (TypeError, ValueError):
            log.warning("[claude_code] app=%s ignoring cli_timeout_s=%r: not a number", app.id, raw)
        else:
            if seconds > 0:
                return seconds
            log.warning("[claude_code] app=%s ignoring cli_timeout_s=%r: not positive", app.id, raw)
    return DEFAULT_TIMEOUT_S


def _resolve_permission_mode(app: AgentApp) -> str:
    cfg = app.gates_config or {}
    mode = cfg.get("cli_permission_mode")
    if mode in ("plan", "acceptEdits", "bypassPermissions", "default"):
        return mode
    return DEFAULT_PERMISSION_MODE


def _resolve_allowed_tools(app: AgentApp) -> list[str] | None:
    cfg = app.gates_config or {}
    tools = cfg.get("cli_allowed_tools")
    if isinstance(tools, list) and tools:
        return [str(t) for t in tools]
    if isinstance(tools, str) and tools:
        return [t.strip() for t in tools.split(",") if t.strip()]
    return None


_DISPATCHER_RE = re.compile(r"Bash\(\./tools call ([A-Za-z0-9_.:-]+)")


def _harness_note(cwd: str, allowed_tools: list[str] | None) -> str:
    """How this agent reaches its tools, when they are behind a dispatcher.

    A SOUL describes what an agent does, not how the machine it happens to be
    running on exposes its tools. Written for a runtime that is handed a tools
    array, it says "use your tools" — and on a CLI harness that sentence is
    true but unactionable: the tools exist, nothing says they are a shell
    command away. Observed directly, and the failure is the expensive kind:
    the agent hunts, finds nothing, and reports a tooling problem to the user.

    So the runtime says it, not each SOUL. That is what keeps one SOUL running
    on every runtime instead of one per harness.
    """
    if not (Path(cwd) / "tools").is_file():
        return ""

    names = []
    for pattern in allowed_tools or []:
        m = _DISPATCHER_RE.search(pattern)
        if m and m.group(1) not in names:
            names.append(m.group(1))

    lines = [
        "## Your tools",
        "",
        "They are a shell command in this directory, not built-in functions. Run them with Bash:",
        "",
        "    ./tools list                     # every tool you have",
        "    ./tools schema <name>            # its arguments",
        "    ./tools call <name> '<json>'     # run it; result on stdout",
        "",
    ]
    if names:
        lines.append("Available to you: " + ", ".join(sorted(names)) + ".")
    lines.append(
        "Call them for anything you would otherwise be guessing at. If one "
        "fails, say what failed rather than answering from memory."
    )
    return "\n".join(lines)


def _build_prompt(registry: AppRegistry, app: AgentApp, message: str, session_id: str) -> str:
    from runspace.protocols.prompt import build_message_envelope

    parts: list[str] = []
    if app._soul_text:
        parts.append(app._soul_text.strip())

    note = _harness_note(_resolve_cwd(app), _resolve_allowed_tools(app))
    if note:
        parts.append(note)

    history = registry._get_history(session_id)
    if history:
        parts.append("## Conversation so far")
        for turn in history:
            role = turn.get("role", "user").upper()
            content = (turn.get("content") or "").strip()
            if content:
                parts.append(f"[{role}]\n{content}")

    # The caller's own name, not the workspace's static one.
    #
    # The agentino runtime reads request_user_name here; this one did not, so a
    # CLI-run agent was told it was speaking to whoever workspace.yml names —
    # "Guest" — however the caller had signed in. The agent therefore could not
    # know it was talking to the owner, and addressed him in the third person
    # no matter what its instructions said. The static name stays as the
    # fallback for a turn with no request bound to it.
    from ..app_registry import request_user_name

    envelope = build_message_envelope(
        message,
        company=registry.workspace_name.replace(" Back Office", "") or None,
        user_name=request_user_name.get(None) or registry._user_name or None,
        user_role=registry._user_role or None,
    )
    parts.append("## Current request")
    parts.append(envelope)
    return "\n\n".join(parts)


_DISPATCHER_CALL = re.compile(r"(?:^|[\s;&|(])(?:\./)?tools\s+call\s+([A-Za-z0-9_.:-]+)")

# The CLI's own refusal when a command falls outside `--allowedTools`. It comes
# back as a `tool_result` with `is_error`, so it is indistinguishable from a
# tool that ran and failed unless the text is read: one is a call the agent
# made, the other is a call that never happened.
_PERMISSION_REFUSED = re.compile(
    r"requires approval|requested permissions|have(?:n't| not) granted", re.I
)


def _tool_display_name(block: dict, own_tools_only: bool = False) -> str:
    """The name to show for a `tool_use` block.

    Claude Code reports its own tools — `Bash`, `Read` — but an agent that
    reaches its real tools through a shell dispatcher (`./tools call <name>`)
    is calling `<name>`; that is what the reader should see, and what
    `tools_used` should record.

    With `own_tools_only` anything else is dropped (returns ""): once a
    workspace has a dispatcher, the CLI's own tools are plumbing — the `ls`
    before the call, the `Read` of a file the tool wrote — and a reader
    shown "Bash, Bash, Bash, lookup_order, Bash" learns nothing from the
    Bashes. Without a dispatcher the CLI's tools are all the agent has, so
    they keep their names.
    """
    name = str(block.get("name") or "")
    if name == "Bash":
        inp = block.get("input")
        command = inp.get("command") if isinstance(inp, dict) else None
        if isinstance(command, str):
            m = _DISPATCHER_CALL.search(command)
            if m:
                return m.group(1)
    return "" if own_tools_only else name


def _has_dispatcher(cwd: str) -> bool:
    """Does this workspace reach its tools through `./tools`?"""
    return (Path(cwd) / "tools").is_file()


class _StreamState:
    """Fold `claude -p --output-format stream-json` events as they arrive.

    Event shapes (from Claude Code SDK docs):
      - {"type": "system", "subtype": "init", ...}
      - {"type": "assistant", "message": {"content": [{"type":"text","text":...},
                                                       {"type":"tool_use","name":...}]}}
      - {"type": "user", "message": {"content": [{"type":"tool_result", ...}]}}
      - {"type": "result", "result": "...", "total_cost_usd": ..., "session_id": ...}

    `feed` returns the tool names a single event announced, so a streaming
    caller can tell the reader what is running while the turn is still going
    — the CLI emits each `tool_use` the moment it happens, and a turn that
    calls tools for minutes is silent otherwise.
    """

    def __init__(self, own_tools_only: bool = False) -> None:
        self.own_tools_only = own_tools_only
        self.final_text = ""
        self.tools_used: list[str] = []
        self.tool_outputs: list[str] = []
        self.meta: dict = {}
        # tool_use id -> reported name, until the matching result arrives.
        self._pending: dict[str, str] = {}
        self.refused: list[str] = []

    def feed(self, ev: dict) -> list[str]:
        ev_type = ev.get("type", "")
        if ev_type == "result":
            res = ev.get("result")
            if isinstance(res, str) and res:
                self.final_text = res
            if "total_cost_usd" in ev:
                self.meta["cost_usd"] = ev["total_cost_usd"]
            if "session_id" in ev:
                self.meta["session_id"] = ev["session_id"]
            return []

        msg = ev.get("message") or {}
        content = msg.get("content") if isinstance(msg, dict) else None
        if not isinstance(content, list):
            return []
        announced: list[str] = []
        for block in content:
            if not isinstance(block, dict):
                continue
            btype = block.get("type", "")
            if btype == "tool_use":
                name = _tool_display_name(block, self.own_tools_only)
                if name:
                    self.tools_used.append(name)
                    announced.append(name)
                    call_id = block.get("id")
                    if isinstance(call_id, str):
                        self._pending[call_id] = name
            elif btype == "tool_result":
                out = block.get("content")
                if isinstance(out, list):
                    out = " ".join(b.get("text", "") for b in out if isinstance(b, dict))
                out = str(out) if out else ""
                name = self._pending.pop(block.get("tool_use_id"), None)
                if block.get("is_error") and _PERMISSION_REFUSED.search(out):
                    # The gate refused it: the agent reached for a tool it is
                    # not allowed and nothing ran. Reporting it as used tells
                    # the reader the answer rests on a call that never
                    # happened, and hands the refusal text to anything reading
                    # tool output as evidence.
                    if name and name in self.tools_used:
                        self.tools_used.remove(name)
                        self.refused.append(name)
                    continue
                if out:
                    self.tool_outputs.append(out[:2000])
            elif btype == "text" and not self.final_text:
                # Fallback: latest assistant text — used only if no `result` event arrives.
                txt = block.get("text", "")
                if isinstance(txt, str) and txt:
                    self.final_text = txt
        return announced


def _parse_claude_stream_json(stdout: str) -> tuple[str, list[str], list[str], dict]:
    """Walk a complete stream-json transcript. Returns
    (final_text, tools_used, tool_outputs, meta)."""
    state = _StreamState()
    for ev in _events_from_lines(stdout.splitlines()):
        state.feed(ev)
    return state.final_text, state.tools_used, state.tool_outputs, state.meta


def _events_from_lines(lines) -> list[dict]:
    events: list[dict] = []
    for line in lines:
        line = line.strip()
        if not line:
            continue
        try:
            ev = json.loads(line)
        except json.JSONDecodeError:
            continue
        if isinstance(ev, dict):
            events.append(ev)
    return events


def _claude_args(
    cwd: str, model: str | None, permission_mode: str, allowed_tools: list[str] | None
) -> list[str]:
    args = [
        _resolve_bin(),
        "-p",
        "--output-format",
        "stream-json",
        "--verbose",  # required by claude -p with stream-json
        "--add-dir",
        cwd,
        "--permission-mode",
        permission_mode,
    ]
    if allowed_tools:
        # Claude Code accepts comma-separated patterns like "Read,Bash(python:*)"
        args += ["--allowedTools", ",".join(allowed_tools)]
    if model:
        args += ["--model", model]
    return args


class _ClaudeRun:
    """One `claude -p` invocation, read as it writes.

    `events()` yields each stream-json event when its line arrives rather
    than after the process exits, so a caller can relay tool calls while the
    turn is in flight. The whole run is bounded by `DEFAULT_TIMEOUT_S`; on
    expiry the process is killed and `asyncio.TimeoutError` is raised from
    the generator. `stderr` is available once the generator is exhausted.
    """

    def __init__(
        self,
        prompt: str,
        cwd: str,
        model: str | None,
        permission_mode: str,
        allowed_tools: list[str] | None = None,
        timeout_s: float = DEFAULT_TIMEOUT_S,
        session_id: str = "",
    ) -> None:
        self.args = _claude_args(cwd, model, permission_mode, allowed_tools)
        self.cwd = cwd
        self.prompt = prompt
        self.timeout_s = timeout_s
        self.session_id = session_id
        self.stderr = ""

    async def events(self) -> AsyncIterator[dict]:
        # The verified caller role, carried into the child's environment.
        #
        # A CLI-run agent reaches its tools through a shell dispatcher, which
        # is a separate process, and a ContextVar cannot cross that boundary —
        # so a tool that gated on the caller saw nothing and failed closed for
        # everyone, including the owner. The environment is per-process, so two
        # turns running at once cannot see each other's role; a module global
        # would let exactly that happen.
        env = dict(os.environ)
        env["RUNSPACE_CALLER_ROLE"] = caller_role.get() or ""
        # The conversation this turn belongs to. A tool that starts work which
        # reports back — a sweep posting its stages and its chart — needs to
        # know where to post, and the session lives in a ContextVar the child
        # process cannot see. Without it the posts went to the runtime's own
        # internal session id and were never displayed anywhere.
        env["RUNSPACE_SESSION_ID"] = self.session_id or ""

        proc = await asyncio.create_subprocess_exec(
            *self.args,
            cwd=self.cwd,
            env=env,
            stdin=asyncio.subprocess.PIPE,
            stdout=asyncio.subprocess.PIPE,
            stderr=asyncio.subprocess.PIPE,
            # One stream-json event per line, and an event carrying a tool
            # result is as large as the result. asyncio's reader defaults to
            # 64 KiB and raises on a longer line, which killed the turn outright
            # — no reply, not even the failure text — the first time a tool
            # returned a few dozen benchmark runs.
            limit=STREAM_LINE_LIMIT,
        )
        # stdin and stderr are pumped in the background: a prompt larger than
        # the pipe would block a plain write until the child read it, and a
        # child that fills stderr would block on us reading stdout.
        feed = asyncio.create_task(self._feed_stdin(proc))
        drain = asyncio.create_task(proc.stderr.read())
        deadline = asyncio.get_running_loop().time() + self.timeout_s
        try:
            while True:
                remaining = deadline - asyncio.get_running_loop().time()
                if remaining <= 0:
                    raise asyncio.TimeoutError
                try:
                    line = await asyncio.wait_for(proc.stdout.readline(), timeout=remaining)
                except ValueError:
                    # Still longer than the ceiling. Losing the turn is bad;
                    # losing it with no reply at all is worse, so stop reading
                    # and let the caller report what it has.
                    log.warning(
                        "[claude_code] cwd=%s a stream line exceeded %d bytes",
                        self.cwd,
                        STREAM_LINE_LIMIT,
                    )
                    break
                if not line:
                    break
                for ev in _events_from_lines([line.decode("utf-8", errors="replace")]):
                    yield ev
            remaining = max(deadline - asyncio.get_running_loop().time(), 0.0)
            await asyncio.wait_for(proc.wait(), timeout=remaining)
        except asyncio.TimeoutError:
            proc.kill()
            await proc.wait()
            raise
        finally:
            # Settle the pumps rather than cancel them: with the child gone
            # both finish at once, and a feed that never got scheduled
            # (short stub output) must still close stdin.
            try:
                await asyncio.wait_for(feed, timeout=5)
            except (asyncio.TimeoutError, Exception):  # noqa: BLE001
                feed.cancel()
            try:
                err = await drain
            except asyncio.CancelledError:
                raise
            except Exception:  # noqa: BLE001 — the pipe is gone with the process
                err = b""
            self.stderr = err.decode("utf-8", errors="replace")

    async def _feed_stdin(self, proc) -> None:
        try:
            proc.stdin.write(self.prompt.encode("utf-8"))
            await proc.stdin.drain()
        except (BrokenPipeError, ConnectionResetError):
            pass
        finally:
            proc.stdin.close()


async def _run_claude(
    prompt: str,
    cwd: str,
    model: str | None,
    permission_mode: str,
    allowed_tools: list[str] | None = None,
    timeout_s: float = DEFAULT_TIMEOUT_S,
) -> tuple[str, str]:
    """Run to completion; returns (stdout, stderr). Kept for callers that
    want the raw transcript rather than events."""
    run = _ClaudeRun(prompt, cwd, model, permission_mode, allowed_tools, timeout_s)
    lines = [json.dumps(ev) async for ev in run.events()]
    return "\n".join(lines) + ("\n" if lines else ""), run.stderr


async def _turn(
    registry: AppRegistry, app: AgentApp, message: str, session_id: str
) -> AsyncIterator[dict]:
    """The shared turn: announce tool calls as they happen, then the reply.

    Yields `{"type": "tool_call", "name": ...}` per tool the CLI starts and
    one final `{"type": "response", ...}`. History is written here so the
    streaming and non-streaming entry points cannot drift.
    """
    get_or_create_agent(registry, app)

    cwd = _resolve_cwd(app)
    _ensure_dispatcher(cwd, app, registry)
    permission_mode = _resolve_permission_mode(app)
    allowed_tools = _resolve_allowed_tools(app)
    prompt = _build_prompt(registry, app, message, session_id)

    registry._add_to_history(session_id, "user", message)
    state = _StreamState(own_tools_only=_has_dispatcher(cwd))
    text = ""
    try:
        model = app.model_for(caller_role.get())
        if model != app.model:
            log.info("[claude_code] app=%s role=%s model=%s", app.id, caller_role.get(), model)
        run = _ClaudeRun(prompt, cwd, model, permission_mode, allowed_tools,
                         _resolve_timeout(app), session_id=session_id)
        async for ev in run.events():
            for name in state.feed(ev):
                yield {"type": "tool_call", "name": name}
        text = state.final_text
        if not text:
            log.warning("[claude_code] no result/text event; stderr=%s", run.stderr.strip()[:500])
            text = failure_text("empty")
        elif state.meta.get("cost_usd") is not None:
            log.info(
                "[claude_code] app=%s cost=$%.4f tools=%d",
                app.id,
                state.meta["cost_usd"],
                len(state.tools_used),
            )
    except asyncio.TimeoutError:
        text = failure_text("timeout")
        log.warning("[claude_code] timeout for app=%s session=%s", app.id, session_id)
    except FileNotFoundError:
        text = failure_text("unavailable")
        log.error("[claude_code] binary missing — set CLAUDE_CODE_BIN or install `claude` CLI")

    if state.refused:
        # A warning, not an info line: an agent reaching for tools it is not
        # allowed is either a gate too tight for the work or a SOUL promising
        # what the configuration does not grant, and both are misconfiguration
        # an operator should see. Deployments commonly leave their loggers at
        # WARNING, where an info line is never printed at all.
        log.warning(
            "[claude_code] app=%s reached for tools it is not allowed: %s",
            app.id,
            ", ".join(sorted(set(state.refused))),
        )

    registry._add_to_history(session_id, "assistant", text)
    yield {
        "type": "response",
        "text": text,
        "tools_used": state.tools_used,
        "tool_outputs": state.tool_outputs,
    }


async def chat(registry: AppRegistry, app: AgentApp, message: str, session_id: str) -> dict:
    result: dict = {"text": "", "tools_used": [], "tool_outputs": []}
    async for ev in _turn(registry, app, message, session_id):
        if ev["type"] == "response":
            result = {k: ev[k] for k in ("text", "tools_used", "tool_outputs")}
    return result


async def stream(
    registry: AppRegistry, app: AgentApp, message: str, session_id: str
) -> AsyncIterator[dict]:
    async for ev in _turn(registry, app, message, session_id):
        yield ev
